name: 'slav_demo'
version: '1.0.0'
config-version: 2

profile: 'default'

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  
clean-targets:         
    - "target"
    - "dbt_modules"

models:
  slav_demo:
    staging:
      materialized: view
      snowflake_warehouse: FIVETRAN_DATABASE
    marts:
      materialized: table
      snowflake_warehouse: FIVETRAN_WAREHOUSE